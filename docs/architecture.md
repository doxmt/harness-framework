# 아키텍처

## 기술 스택

{언어, 프레임워크, 주요 라이브러리}

## 디렉토리 구조

```

```

## 패턴

{사용하는 디자인 패턴 (예: Server Components 기본, 인터랙션이 필요한 곳만 Client Component)}

## 데이터 흐름

```
{데이터가 어떻게 흐르는지 (예:
사용자 입력 → Client Component → API Route → 외부 API → 응답 → UI 업데이트
)}
```

## 네이밍 규칙

- **파일/디렉토리**: {예: kebab-case (user-profile.ts)}
- **컴포넌트**: {예: PascalCase (UserProfile.tsx)}
- **함수/변수**: {예: camelCase (getUserProfile)}
- **상수**: {예: UPPER_SNAKE_CASE (MAX_RETRY_COUNT)}
- **타입/인터페이스**: {예: PascalCase, Interface는 I 접두사 없음}
- {프로젝트 고유 규칙}
